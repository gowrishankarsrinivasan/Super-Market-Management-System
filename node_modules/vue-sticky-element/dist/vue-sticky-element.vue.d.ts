import { type PropType } from 'vue';
declare const _default: import("vue").DefineComponent<{
    visibleOnDirection: {
        type: PropType<"up" | "down" | "disabled">;
        default: string;
        validator: (val: string) => boolean;
    };
    stickMode: {
        type: PropType<"element-end" | "element-start">;
        default: string;
        validator: (val: string) => boolean;
    };
    stuckClass: {
        type: StringConstructor;
        default: string;
    };
    showClass: {
        type: StringConstructor;
        default: string;
    };
    hideClass: {
        type: StringConstructor;
        default: string;
    };
    transitionClass: {
        type: StringConstructor;
        default: string;
    };
    transitionDuration: {
        type: NumberConstructor;
        default: number;
    };
    /** how much user has to scroll back in the opposite direction before element shows again.
     * this is especially important on mobile devices, when user is holding touch on screen,
     * which causes element to show and hide multiple times in a row.
     *
     * resets on scroll in opposite direction of `visibleOnDirection`
     */
    scrollBackThreshold: {
        type: NumberConstructor;
        default: number;
    };
    /** When true, stops checking for scroll positions (essentially, does not do anything). this can help when you need to freeze and scroll the navbar. */
    skipChecks: {
        type: BooleanConstructor;
        default: boolean;
    };
    /** force applies the show class */
    forceShow: {
        type: BooleanConstructor;
        default: boolean;
    };
    /** The element to add `onscroll` event listener to instead of window. this is useful for native apps like ionic where scrolling element might not be window. this can be changed in runtime and the change will be detected, so for example you can get your element in `onMounted` [using `getScrollElement`](https://ionicframework.com/docs/api/content#getscrollelement) and you will be fine. */
    scrollElement: {
        type: PropType<HTMLElement | undefined>;
        default: undefined;
    };
}, unknown, {
    navbarStuck: boolean;
    navbarShow: boolean;
    applyTransition: boolean;
    height: undefined;
    forceHide: boolean;
    observer: ResizeObserver | ((...any: any[]) => void) | undefined;
    lastScrollPos: undefined;
    scrollBackValue: undefined;
}, {
    alwaysStick(): boolean;
    shouldApplyTransition(): boolean;
    stickWithElementStart(): boolean;
    directiveThreshold(): 0;
}, {
    addHide(): void;
    removeHide(): void;
    toggleStickiness(relativeScrollPosToElement: number, goingStickyDirection: boolean): void;
    crossBeforeUnmount(): void;
}, import("vue").ComponentOptionsMixin, import("vue").ComponentOptionsMixin, ("stuck" | "show")[], "stuck" | "show", import("vue").PublicProps, Readonly<import("vue").ExtractPropTypes<{
    visibleOnDirection: {
        type: PropType<"up" | "down" | "disabled">;
        default: string;
        validator: (val: string) => boolean;
    };
    stickMode: {
        type: PropType<"element-end" | "element-start">;
        default: string;
        validator: (val: string) => boolean;
    };
    stuckClass: {
        type: StringConstructor;
        default: string;
    };
    showClass: {
        type: StringConstructor;
        default: string;
    };
    hideClass: {
        type: StringConstructor;
        default: string;
    };
    transitionClass: {
        type: StringConstructor;
        default: string;
    };
    transitionDuration: {
        type: NumberConstructor;
        default: number;
    };
    /** how much user has to scroll back in the opposite direction before element shows again.
     * this is especially important on mobile devices, when user is holding touch on screen,
     * which causes element to show and hide multiple times in a row.
     *
     * resets on scroll in opposite direction of `visibleOnDirection`
     */
    scrollBackThreshold: {
        type: NumberConstructor;
        default: number;
    };
    /** When true, stops checking for scroll positions (essentially, does not do anything). this can help when you need to freeze and scroll the navbar. */
    skipChecks: {
        type: BooleanConstructor;
        default: boolean;
    };
    /** force applies the show class */
    forceShow: {
        type: BooleanConstructor;
        default: boolean;
    };
    /** The element to add `onscroll` event listener to instead of window. this is useful for native apps like ionic where scrolling element might not be window. this can be changed in runtime and the change will be detected, so for example you can get your element in `onMounted` [using `getScrollElement`](https://ionicframework.com/docs/api/content#getscrollelement) and you will be fine. */
    scrollElement: {
        type: PropType<HTMLElement | undefined>;
        default: undefined;
    };
}>> & {
    onStuck?: ((...args: any[]) => any) | undefined;
    onShow?: ((...args: any[]) => any) | undefined;
}, {
    visibleOnDirection: "up" | "down" | "disabled";
    stickMode: "element-end" | "element-start";
    stuckClass: string;
    showClass: string;
    hideClass: string;
    transitionClass: string;
    transitionDuration: number;
    scrollBackThreshold: number;
    skipChecks: boolean;
    forceShow: boolean;
    scrollElement: HTMLElement | undefined;
}, {}>;
export default _default;
